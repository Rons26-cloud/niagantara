# Supabase foundation

Migrations are intentionally ordered from extensions, profiles, companies, memberships, stores, branches, roles, permissions, and role-permissions. RLS is introduced in the first foundation phase before business modules.
