import{BadRequestException,Injectable}from'@nestjs/common';import{ExpensesRepository}from'./expenses.repository.js';import type{CategoryInput,ExpenseInput,ExpenseQuery}from'./dto/expense.dto.js';@Injectable()export class ExpensesService{constructor(private readonly repo:ExpensesRepository){}async list(c:string,q:ExpenseQuery){const{data,error}=await this.repo.list(c,q);if(error)throw error;return data??[]}async categories(c:string){const{data,error}=await this.repo.categories(c);if(error)throw error;return data??[]}async category(c:string,u:string,d:CategoryInput){if(!d.name?.trim())throw new BadRequestException({code:'CATEGORY_NAME_REQUIRED',message:'Category name is required.'});const{data,error}=await this.repo.createCategory(c,u,d);if(error)throw error;return data}async create(c:string,u:string,d:ExpenseInput){if(!Number.isFinite(d.amount)||d.amount<=0||!d.idempotencyKey)throw new BadRequestException({code:'INVALID_EXPENSE',message:'Positive amount and idempotency key are required.'});const{data,error}=await this.repo.create(c,u,d);if(error)throw error;return{id:data}}}
