import{Injectable}from'@nestjs/common';import{SupabaseService}from'../../integrations/supabase/supabase.service.js';import type{ClockInput}from'./dto/attendance.dto.js';@Injectable()export class AttendanceRepository{constructor(private readonly db:SupabaseService){}list(c:string,branch?:string,allowedBranches?:string[]){let q=this.db.client.from('attendance_records').select('*,employee:employees(id,name,employee_code),branch:branches(id,name)').eq('company_id',c).order('clock_in_at',{ascending:false}).limit(100);if(branch)q=q.eq('branch_id',branch);else if(allowedBranches)q=q.in('branch_id',allowedBranches);return q}clock(c:string,u:string,d:ClockInput){return this.db.client.rpc('clock_employee',{target_company_id:c,target_employee_id:d.employeeId,target_branch_id:d.branchId,actor_id:u,target_action:d.action,target_notes:d.notes??null})}}
