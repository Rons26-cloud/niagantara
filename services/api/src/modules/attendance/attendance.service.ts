import{BadRequestException,Injectable}from'@nestjs/common';import{AttendanceRepository}from'./attendance.repository.js';import type{ClockInput}from'./dto/attendance.dto.js';@Injectable()export class AttendanceService{constructor(private readonly repo:AttendanceRepository){}async list(c:string,b?:string){const{data,error}=await this.repo.list(c,b);if(error)throw error;return data??[]}async clock(c:string,u:string,d:ClockInput){if(!['CLOCK_IN','CLOCK_OUT'].includes(d.action))throw new BadRequestException({code:'INVALID_CLOCK_ACTION',message:'Clock action is invalid.'});const{data,error}=await this.repo.clock(c,u,d);if(error)throw error;return data}}
