import{BadRequestException,ForbiddenException,Injectable}from'@nestjs/common';import{AttendanceRepository}from'./attendance.repository.js';import type{ClockInput}from'./dto/attendance.dto.js';@Injectable()export class AttendanceService{constructor(private readonly repo:AttendanceRepository){}async list(c:string,b?:string,allowedBranches?:string[]){if(b&&allowedBranches&&!allowedBranches.includes(b))throw new ForbiddenException({code:'BRANCH_ACCESS_DENIED',message:'Attendance branch is outside your scope.'});if(allowedBranches?.length===0)return[];const{data,error}=await this.repo.list(c,b,allowedBranches);if(error)throw error;return data??[]}async clock(c:string,u:string,d:ClockInput,allowedBranches?:string[]){if(allowedBranches&&!allowedBranches.includes(d.branchId))throw new ForbiddenException({code:'BRANCH_ACCESS_DENIED',message:'Attendance branch is outside your scope.'});if(!['CLOCK_IN','CLOCK_OUT'].includes(d.action))throw new BadRequestException({code:'INVALID_CLOCK_ACTION',message:'Clock action is invalid.'});const{data,error}=await this.repo.clock(c,u,d);if(error)throw error;return data}}
