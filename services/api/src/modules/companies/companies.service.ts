import { Injectable,ForbiddenException,NotFoundException } from '@nestjs/common'; import { SupabaseService } from '../../integrations/supabase/supabase.service.js'; import { AuditService } from '../audit/audit.service.js'; import { CreateCompanyDto,UpdateCompanyDto } from './dto/company.dto.js'; @Injectable() export class CompaniesService{constructor(private readonly db:SupabaseService,private readonly audit:AuditService){} async list(userId:string){const {data,error}=await this.db.client.from('company_members').select('company:companies(*)').eq('user_id',userId).eq('status','active'); if(error) throw error; return (data??[]).map((r:any)=>r.company).filter(Boolean)} async get(userId:string,id:string){const {data,error}=await this.db.client.from('companies').select('*').eq('id',id).maybeSingle(); if(error) throw error; if(!data) throw new NotFoundException({code:'RESOURCE_NOT_FOUND',message:'Company not found.'}); const {data:m}=await this.db.client.from('company_members').select('id').eq('company_id',id).eq('user_id',userId).eq('status','active').maybeSingle(); if(!m) throw new ForbiddenException({code:'TENANT_ACCESS_DENIED',message:'You do not have access to this company.'}); return data} async create(userId:string,dto:CreateCompanyDto){const {data,error}=await this.db.client.rpc('provision_company',{p_user_id:userId,p_company_name:dto.name,p_legal_name:dto.legalName??null}); if(error) throw error; const company=(data as any)?.company??data; await this.audit.record({action:'company.create',resourceType:'company',resourceId:company?.id,actorUserId:userId,companyId:company?.id}); return company} async update(userId:string,id:string,dto:UpdateCompanyDto){await this.get(userId,id); const {data,error}=await this.db.client.from('companies').update(dto).eq('id',id).select().single(); if(error) throw error; await this.audit.record({action:'company.update',resourceType:'company',resourceId:id,actorUserId:userId,companyId:id}); return data}}