import { Injectable,UnauthorizedException,ServiceUnavailableException } from '@nestjs/common'; import { SupabaseService } from '../../integrations/supabase/supabase.service.js'; import { RegisterDto,LoginDto,ForgotPasswordDto,ResetPasswordDto } from './dto/auth.dto.js'; @Injectable() export class AuthService { constructor(private readonly supabase:SupabaseService){} async register(dto:RegisterDto){const {data,error}=await this.supabase.client.auth.signUp({email:dto.email,password:dto.password}); if(error) throw new UnauthorizedException({code:'REGISTER_FAILED',message:'Unable to create account.'}); return {user:data.user,session:data.session,provisioning:'pending'};} async login(dto:LoginDto){const {data,error}=await this.supabase.client.auth.signInWithPassword(dto); if(error||!data.user) throw new UnauthorizedException({code:'INVALID_CREDENTIALS',message:'Invalid email or password.'}); return {user:data.user,session:data.session};} async logout(accessToken:string){const {error}=await this.supabase.client.auth.admin.signOut(accessToken); if(error) throw new UnauthorizedException({code:'INVALID_SESSION',message:'Session could not be revoked.'}); return {success:true};} async forgotPassword(dto:ForgotPasswordDto){await this.supabase.client.auth.resetPasswordForEmail(dto.email); return {accepted:true};} async resetPassword(_dto:ResetPasswordDto){throw new ServiceUnavailableException({code:'RESET_REQUIRES_SESSION',message:'Password reset requires a Supabase recovery session.'});} async me(userId:string){const {data:profile}=await this.supabase.client.from('profiles').select('*').eq('id',userId).maybeSingle(); const {data:companies}=await this.supabase.client.from('company_members').select('company_id,role_key,status').eq('user_id',userId).eq('status','active'); return {user:{id:userId},profile,companies:companies??[],active_company:null,roles:(companies??[]).map((m:any)=>m.role_key),permissions:[],accessible_branches:[]};}}