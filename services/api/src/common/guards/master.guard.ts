import { CanActivate,ExecutionContext,ForbiddenException,Injectable } from '@nestjs/common'; @Injectable() export class MasterGuard implements CanActivate { canActivate(ctx:ExecutionContext){const req=ctx.switchToHttp().getRequest(); if(!req.authz?.platformRole) throw new ForbiddenException({code:'MASTER_ACCESS_DENIED',message:'Platform-level access is required.'}); return true;}}