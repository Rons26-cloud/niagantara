import{readdirSync,readFileSync}from'node:fs';const dir=new URL('../supabase/migrations/',import.meta.url);const files=readdirSync(dir).filter(x=>x.endsWith('.sql')).sort();const ids=files.map(x=>x.split('_')[0]);if(new Set(ids).size!==ids.length)throw new Error('Duplicate migration identifier.');for(const file of files){const sql=readFileSync(new URL(file,dir),'utf8');if(/drop\s+(table|schema)|truncate\s+/i.test(sql))throw new Error(`Destructive SQL requires manual review: ${file}`);if(/security\s+definer/i.test(sql)&&!/set\s+search_path/i.test(sql))throw new Error(`SECURITY DEFINER lacks fixed search_path: ${file}`);}console.log(`Migration static validation PASS (${files.length} files)`);
